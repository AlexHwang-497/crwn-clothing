
// * we are exporting an object called userAction types 
    // * on it will be the value that points to the exact string and now in our producer, we are just going to import that in
export const UserActionTypes = {
    SET_CURRENT_USER: 'SET_CURRENT_USER'
  };